# HistoryMinerva 文档中心

## 现行文档

- [`项目概览.md`](./current/项目概览.md)：范围、状态和交付物。
- [`技术合同.md`](./current/技术合同.md)：HistoryMinerva 4.3.0 需求、宿主和实现边界。
- [`有效决策.md`](./current/有效决策.md)：长期有效的 CAD 与宿主决策。
- [`验证合同.md`](./current/验证合同.md)：构建、Smoke、UI、包和部署验收入口。
- [`HistoryMinerva转换架构.drawio`](./current/HistoryMinerva转换架构.drawio)：内部 HistoryMinerva 转换架构图。

## 模块接口

- [`模块API.md`](./package/模块API.md)：模块对外消费面（命令、Worker 协议、建模 API、数据目录）。
  NuGet 打包暂不开放，OHS 旧宿主已停用；构建与部署验收见 `current/验证合同.md`。

## 历史资料

`history/` 保存全部版本化文档与迁移前的设计、COM 研究和验收记录：编号文档按版本时序追加
（最新为 `45-V4.3.0-SW自整备转换管线.md`），`swuse-v0.1/` 保存 SWuse 并入前的完整档案。
历史文档保留当时路径和部署来源，不作为默认 AI 上下文，也不覆盖现行合同。

## 目录规范

根目录只使用 `a-*`、`b-*`、`z-*` 项目目录：`b-Code-HistoryMinerva` 是生产模块工程（前端 +
Contracts/Api + 合并 Worker），`b-Code-HistoryMinerva-Tests` 是独立测试与真机门禁工程，
`z-HistoryMinerva` 是唯一发布目录（模块清单与运行产物同处，由发布脚本生成）。
