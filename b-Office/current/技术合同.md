# Mapping 4.1.0 技术合同

`history/` 只保存迁移前事实。本文件是 Mapping 当前实现和验收的唯一技术真值。

## REQ-001 对外身份与兼容边界

- 对外模块名、窗口 ID、窗口标题和命令域为 `Mapping` / `mapping`，版本由
  `b-Code-HistoryMinerva/build/HistoryMinerva.Version.props` 唯一决定。
- 现有只读入口 `mapping.status/paths/scan/assemblyPaths/assemblyProbe/show/hide` 保持兼容。
- `HistoryMinerva.convert`、`HistoryMinerva.cancel` 是前端写命令，要求 UI 线程，`Readonly=false`、
  `AllowMcpExecution=false`；服务端前端代理负责跨进程转发，MCP 不出现这两个命令。
- SE2SW 程序集、Worker 文件名、Worker JSON 请求/事件/结果协议、COM 调用和输出目录规则不变。

## REQ-002 通用两栏 UI

- 页面根使用透明背景，宿主决定外层 Canvas；页面使用 `Shell.Brush.*`、`Shell.Font.*`、`Shell.Space.*`、
  `Shell.Radius.*`、`Shell.Size.*` 动态令牌，不在生产 XAML 固定颜色、字体或第二套控件模板。
- 顶部为两个等宽栏：转换来源、转换内容。转换内容只有 `.par → .SLDPRT` 和 `.asm → .SLDASM`。
- Mapping 是 `DockSide.Center` 单例中央业务页，页面不创建自己的窗口标题、停靠区或窗口按钮。
- 320px、浅色/深色和 100%/125%/150% DPI 下，来源选择、内容选择、选项和主操作保持可见可操作；长路径和映射名以省略号或换行处理。

## REQ-003 宿主上下文与数据

- UI 模块和命令类实现 `IModuleContextAware`，使用宿主 `CommandBus`、`IShellLog`、设置和数据根。
- `MappingRuntimePaths` 从 `IModuleContext.DataDirectory` 与 `module.dir` 推导模块数据目录、双槽路径、请求和探查目录。
- Worker 定位顺序为当前程序集目录、宿主进程基目录、`module.dir/Mapping` 部署槽；找不到时返回首候选供预检报告。
- 源 CAD 文件只读；扫描、探查和纯内存规划不创建 XT/SW 输出目录，环境检查通过后才创建。

## REQ-004 CAD 与生命周期

- Solid Edge `SaveBody` 输出 Parasolid `.x_t`，SolidWorks 导入并保存 `.SLDPRT/.SLDASM`；装配层级、矩阵、特征守卫和配合回滚沿用既有 3.6.7 真机合同。
- UI 不持有 CAD COM 对象；所有 COM 工作由独立 x64 STA Worker 完成。取消和卸载必须等待 Worker 收束，不终止宿主已有 CAD 会话。

## 依赖与发布

- .NET 8、Windows Desktop Runtime、Solid Edge 2020 COM、SolidWorks 2025 SP5 COM、FeatureWorks。
- AppShell 3.1.9 正式消费快照为 `../2026-023-AppShell/z-Package-AppShell`；manifest SHA256 为
  `9D27B06100988DE89547931201E5612487C5ADA3C14C9D7277BFFA1C1450E72A`，Core SHA256 为
  `0EA9D28EEE8AB28A20679042B937A4C94A288177B543602E50C30EDC2115A58B`。
- `z-HistoryMinerva` 是唯一发布目录（清单与运行产物同处），只由 `Build-HistoryMinervaPackage.ps1` 从 Release 输出生成；部署由 `Deploy-HistoryMinerva.ps1` 控制，
  备份必须位于 Modules 目录之外。
