# HistoryMinerva 4.3.17 验证合同

| 编号 | 命令或步骤 | 通过标准 |
| --- | --- | --- |
| VERIFY-QUALITY | `.\b-Code-HistoryMinerva\eng\Test-QualityGate.ps1` | 共享项目合同、抑制/热点、版本、9 条命令/API、正式 z 边界与 SHA、HistoryVulcan 最低版本 3.11.1 及程序集身份全部通过 |
| VERIFY-VERSION | 读取 `HistoryMinerva.Version.props`、源码 `module.manifest.json` 与正式 z 清单 | 源码与源码清单为 4.3.17；正式 Clio z 在本工作区合并前可为 4.3.16；旧版本只存在于发布历史 |
| VERIFY-BUILD | `dotnet build .\HistoryMinerva.sln -c Debug/Release --no-restore -warnaserror -p:NuGetAudit=false` | 两配置均 0 警告、0 错误，引用 HistoryVulcan.Core 3.11.1.0 且 `Private=false` |
| VERIFY-SMOKE | `dotnet run --project .\b-Code-HistoryMinerva-Tests\tests\HistoryMinerva.Smoke\HistoryMinerva.Smoke.csproj -c Release -p:NuGetAudit=false` | 输出 `HistoryMinerva.Smoke: PASS`，身份、上下文、命令、生命周期、SWuse 协议路由、SW 特征整备不复用已有 SLDPRT 且一律经 XT 再识别、快捷方式解析、前置错误进入失败结果、柔性子装配按在位姿态生成、属性整备图号规则与按空格洗图号通过 |
| VERIFY-UI | `dotnet run --project .\b-Code-HistoryMinerva-Tests\tests\HistoryMinerva.UiSmoke\HistoryMinerva.UiSmoke.csproj ...` | 加载真实 HistoryVulcan 浅/深主题，覆盖 320/760/1200px；顶栏等宽、来源/内容标签与选择控件同列，零件列表无独立标题行，宽窗口操作栏单行且窄窗口无水平溢出 |
| VERIFY-FORMAT | `dotnet format .\HistoryMinerva.sln --verify-no-changes --no-restore` | 四个生产工程、Smoke 与 UI Smoke 均无格式差异 |
| VERIFY-PACKAGE | `powershell -NoProfile -ExecutionPolicy Bypass -File .\b-Code-HistoryMinerva\eng\Build-HistoryMinervaPackage.ps1` | HistoryVulcan 宿主不低于 3.11.1，十个运行产物和 SHA256SUMS 一致，无宿主 DLL |
| VERIFY-DEPLOY | Diana 集中发布命令 `-Module HistoryMinerva -Publish` 后复核 | Z 快照哈希与正式包一致，旧 Mapping、SWuse 与历史转换模块槽退役且备份不在 Modules |
| VERIFY-CAD | `AssemblyProductionGate` / `FeatureWorksBatchSmoke` / `SolidWorksSelfPipelineGate` / `SWuse.CadGate` | `tools/` 只保留四个可重复执行的端到端门禁；Solid Edge → SolidWorks 链路本次不重跑，沿用 Worker/COM 未改变的既有真机证据 |
| VERIFY-SW-SELF | `SolidWorksSelfPipelineGate --assembly <源.SLDASM> --worker <HistoryMinerva.Worker.exe> --out-dir <输出目录>` | 退出码 0；实例数与源一致且全部配对，最大平移偏差 < 1e-6 m、最大旋转偏差 < 1e-9；产物装配 open errors=0；源目录出现交付用 `XT/` 与 `SW/`；有特征的普通零件与哑实体都先落到 XT 再识别；识别失败或几何被改坏的零件为 Parasolid 导入体而非源特征树；源目录文件与源装配 SHA256 一字未改 |
| VERIFY-CI | `.github/workflows/historyminerva-gate.yml` | push/PR 上锁定还原、质量门禁、双配置构建与 Smoke、窄/宽 UI Smoke、格式、候选包及无 tracked diff 全绿 |

Smoke 还检查后台只注册五条 `minerva.worker.*` 只读命令且没有 `HistoryMinerva.*`；UI Smoke 检查
`minerva.conversion.probe/run/strip/cancel` 的只前端、禁止 MCP 元数据，Worker 进度、失败和取消均进入命令日志，
页面不再显示全局提示，以及服务宿主无 ShellUi 时不重复注册。

VERIFY-SW-SELF 需要真实 SolidWorks + FeatureWorks 会话。FeatureWorks 的自动识别依赖**进程内激活态**：
该 SolidWorks 进程里必须先人工做过一次特征识别，否则整批只会产出压平后的导入体（几何与装配位置不受影响）。
本版真机证据见 `b-Office/history/45-V4.3.0-SW自整备转换管线.md` §5。带真实配合的 SW 装配体样件尚未取得，
SW 源的配合重建只有离线契约测试覆盖。

云端 CI 明确不启动 SolidWorks、Solid Edge 或 FeatureWorks，也不运行 `VERIFY-SW-SELF`；这些检查只有在
具备 CAD 授权、桌面会话、样件和 FeatureWorks 进程内激活态时才有有效结论。

正式发布默认先部署后提交。只有要从干净 HEAD 复现时，才为本次管线追加 `-RequireCleanSource`。
