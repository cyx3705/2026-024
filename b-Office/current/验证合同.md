# Mapping 4.1.0 验证合同

| 编号 | 命令或步骤 | 通过标准 |
| --- | --- | --- |
| VERIFY-CONTRACT | `powershell -NoProfile -ExecutionPolicy Bypass -File .\b-Code\Test-ProjectContract.ps1 -Instantiation` | manifest、路径、链接、目录和占位符检查通过 |
| VERIFY-BUILD | `dotnet build .\b-Code-SE2SW\src\SE2SW\SE2SW.csproj -c Release -p:NuGetAudit=false` | 0 警告、0 错误，引用 AppShell.Core 3.1.9.0 且 `Private=false` |
| VERIFY-SMOKE | `dotnet run --project .\b-Code-SE2SW-Tests\tests\SE2SW.Smoke\SE2SW.Smoke.csproj -c Release -p:NuGetAudit=false` | 输出 `SE2SW.Smoke: PASS`，身份、上下文、命令和生命周期通过 |
| VERIFY-UI | `dotnet run --project .\b-Code-SE2SW-Tests\tests\SE2SW.UiSmoke\SE2SW.UiSmoke.csproj ...` | 加载真实 AppShell 浅/深主题，覆盖 320/760/1200px，顶栏等宽且无固定色/溢出 |
| VERIFY-FORMAT | `dotnet format .\b-Code-SE2SW\src\SE2SW\SE2SW.csproj --verify-no-changes --no-restore` | 无格式差异 |
| VERIFY-PACKAGE | `powershell -NoProfile -ExecutionPolicy Bypass -File .\b-Code-SE2SW\eng\Build-MappingPackage.ps1` | AppShell 3.1.9 版本与两项 SHA 固定，七个运行产物和 SHA256SUMS 一致，无宿主 DLL |
| VERIFY-DEPLOY | `powershell ... .\b-Code-SE2SW\eng\Deploy-Mapping.ps1`、`-Apply` 后复核 | 双槽哈希与正式包一致，旧 OHS SE2SW 槽退役且备份不在 Modules |
| VERIFY-CAD | `b-Code-SE2SW-Tests/tools` 专用样件 | 本次不重跑；沿用 Worker/COM 未改变的既有真机证据 |

UI Smoke 还检查 `mapping.convert/cancel` 的只前端、非只读、禁止 MCP 元数据以及服务宿主无 ShellUi 时不重复注册。
