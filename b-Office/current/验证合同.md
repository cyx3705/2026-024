# HistoryMinerva 4.6.10 验证合同

| 编号 | 命令或步骤 | 通过标准 |
| --- | --- | --- |
| VERIFY-QUALITY | `.\b-Code-HistoryMinerva\eng\Test-QualityGate.ps1` | 共享项目合同、抑制/热点、版本、9 条命令/API、正式 z 边界与 SHA、HistoryVulcan 最低版本 5.1.2 及程序集身份全部通过 |
| VERIFY-VERSION | 读取 `HistoryMinerva.Version.props`、源码 `module.manifest.json` 与候选 z 清单 | 源码与源码清单为 4.6.10；旧版本只存在于发布历史 |
| VERIFY-BUILD | `dotnet build .\HistoryMinerva.sln -c Debug/Release --no-restore -warnaserror -p:NuGetAudit=false` | 两配置均 0 警告、0 错误，引用 HistoryVulcan.Core 5.1.2.0 且 `Private=false` |
| VERIFY-SMOKE | `dotnet run --project .\b-Code-HistoryMinerva-Tests\tests\HistoryMinerva.Smoke\HistoryMinerva.Smoke.csproj -c Release -p:NuGetAudit=false` | 输出 `HistoryMinerva.Smoke: PASS`，身份、上下文、命令、生命周期、SW 特征整备不复用已有 SLDPRT 且一律经 XT 再识别、生产代码不含「跳过整备」或「源零件已有特征树」、快捷方式解析、前置错误进入失败结果、柔性子装配按在位姿态生成、属性整备图号规则与按空格洗图号通过；选错文件类型时 `minerva.ui.source` 仍成功、说明未更改来源且已选装配不被清掉；解析装配体跳过子文件夹外购件；来源选择器走 `minerva.ui.picksource`，对话框从桌面打开且不核验网盘路径 |
| VERIFY-UI | `dotnet run --project .\b-Code-HistoryMinerva-Tests\tests\HistoryMinerva.UiSmoke\HistoryMinerva.UiSmoke.csproj ...` | 加载 HistoryAurora 1.0.0 Release 运行时的真实浅/深主题，覆盖 320/760/1200px；顶栏等宽、来源/内容标签与选择控件同列，零件列表无独立标题行，装配结构默认收起且从左下角「打开」自左侧弹出，零件表铺满正文宽度，宽窗口操作栏单行且窄窗口无水平溢出 |
| VERIFY-FORMAT | `dotnet format .\HistoryMinerva.sln --verify-no-changes --no-restore` | 四个生产工程、Smoke 与 UI Smoke 均无格式差异 |
| VERIFY-PACKAGE | `powershell -NoProfile -ExecutionPolicy Bypass -File .\b-Code-HistoryMinerva\eng\Build-HistoryMinervaPackage.ps1` | HistoryVulcan 宿主不低于 5.1.2，七个运行产物和 SHA256SUMS 一致，无宿主 DLL、无 HistoryMinerva.Api、无 Roslyn |
| VERIFY-DEPLOY | 宿主 `vulcan.dev.submit` / `finish` 后复核 | `z-Publish/HistoryMinerva-vX.Y.Z/` 哈希与正式包一致；包内无 HistoryMinerva.Api.dll 与 Roslyn |
| VERIFY-CAD | `AssemblyProductionGate` / `FeatureWorksBatchSmoke` / `SolidWorksSelfPipelineGate` | `tools/` 只保留三个可重复执行的端到端门禁；Solid Edge → SolidWorks 链路本次不重跑，沿用 Worker/COM 未改变的既有真机证据 |
| VERIFY-SW-SELF | `SolidWorksSelfPipelineGate --assembly <源.SLDASM> --worker <HistoryMinerva.Worker.exe> --out-dir <输出目录>` | 退出码 0；实例数与源一致且全部配对，最大平移偏差 < 1e-6 m、最大旋转偏差 < 1e-9；产物装配 open errors=0；每个零件都有非空 Parasolid 文本 `.x_t` 落在 `XT/`；Worker 事件不含「跳过整备」或「源零件已有特征树」；产物不得与源零件字节相同；识别失败或几何被改坏的零件为 Parasolid 导入体而非源特征树；源目录文件与源装配 SHA256 一字未改 |
| VERIFY-CI | `.github/workflows/historyminerva-gate.yml` | push/PR 上锁定还原、质量门禁、双配置构建与 Smoke、窄/宽 UI Smoke、格式、候选包及无 tracked diff 全绿 |

Smoke 还检查后台注册五条 `minerva.worker.*` 只读命令、四条 `minerva.conversion.*`（禁止 MCP）、
`minerva.ui.describe/data/actions/picksource` 描述式协议（禁止 MCP）且没有
`HistoryMinerva.*`；转换指令在无 ShellUi 的 Attach 阶段也必须登记，否则热重载后页面报未知指令。
UI Smoke 检查 Worker 进度、失败和取消均进入命令日志，页面不再显示全局提示。

VERIFY-SW-SELF 需要真实 SolidWorks + FeatureWorks 会话。FeatureWorks 的自动识别依赖**进程内激活态**：
该 SolidWorks 进程里必须先人工做过一次特征识别，否则整批只会产出压平后的导入体（几何与装配位置不受影响）。
本版真机证据见 `b-Office/history/45-V4.3.0-SW自整备转换管线.md` §5。带真实配合的 SW 装配体样件尚未取得，
SW 源的配合重建只有离线契约测试覆盖。

云端 CI 明确不启动 SolidWorks、Solid Edge 或 FeatureWorks，也不运行 `VERIFY-SW-SELF`；这些检查只有在
具备 CAD 授权、桌面会话、样件和 FeatureWorks 进程内激活态时才有有效结论。

正式发布默认先部署后提交。只有要从干净 HEAD 复现时，才为本次管线追加 `-RequireCleanSource`。
