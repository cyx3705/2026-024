# HistoryMinerva 4.10.1 验证合同

| 编号 | 命令或步骤 | 通过标准 |
| --- | --- | --- |
| VERIFY-QUALITY | `.\b-Code-HistoryMinerva\eng\Test-QualityGate.ps1` | 共享项目合同、抑制/热点、版本、17 条命令/API、正式 z 边界与 SHA、HistoryVulcan 最低版本 5.1.2 及程序集身份全部通过 |
| VERIFY-VERSION | 读取 `HistoryMinerva.Version.props`、源码 `module.manifest.json` 与候选 z 清单 | 源码与源码清单为 4.10.1；旧版本只存在于发布历史 |
| VERIFY-BUILD | `dotnet build .\HistoryMinerva.sln -c Debug/Release --no-restore -warnaserror -p:NuGetAudit=false` | 两配置均 0 警告、0 错误，引用 HistoryVulcan.Core 5.1.2.0 且 `Private=false` |
| VERIFY-SMOKE | `dotnet run --project .\b-Code-HistoryMinerva-Tests\tests\HistoryMinerva.Smoke\HistoryMinerva.Smoke.csproj -c Release -p:NuGetAudit=false` | 输出 `HistoryMinerva.Smoke: PASS`，身份、上下文、命令、生命周期、SW 特征整备不复用已有 SLDPRT 且一律经 XT 再识别、生产代码不含「跳过整备」或「源零件已有特征树」、快捷方式解析、前置错误进入失败结果、柔性子装配按在位姿态生成、属性整备图号规则通过、`minerva.conversion.*` 只剩 probe/run/cancel 三条；空前缀是删图号不得阻断且每条图号为空、顶部不得再留一个同义按钮；既有图号按文件名第一个空格切开，不合本项目规则的旧号段（如 `QT-88(旧)`）也照样认出来；写入只把属性写到拿到图号的 `.SLDPRT`、名字已合规时写入按钮仍可点、整份清单送 Worker、改图号前缀不冲掉已填的材料、没填的槽不写成空串、删图号那一轮只把「图号」清成空串、「材料」槽写的是链接记号 `SW-Material` 而非材质名文本、材质名缺材料库当场报错、表面处理与热处理候选在读不到模板时仍有兜底；属性槽为八个且「名称」写文件名里图号之后的那一段原零件名称、改名条目带 `PartName`；解析读回的三个槽直接预填到零件表且用户改过的那一格盖过它、没动过的按原值写回（含材料库）；改名后索引自动跟到新文件名（来源装配、零件行、记账全部跟上，第二次写入不被过期哈希挡住）；改图号前缀原地更新同一行、行 Id 前后一致；零件表是图号、名称、状态加三个可点属性列（V4.9 把「文件」拆成图号与名称），页面不再有「改名后预览」列；选错文件类型时 `minerva.ui.source` 仍成功、说明未更改来源且已选装配不被清掉；解析装配体跳过子文件夹外购件；来源选择跟转换内容走，装配体选文件、零件选文件夹，选完不解析；整体打包为第五种转换内容，选中后三个转换开关隐藏、主按钮为「打包」；打包表只列零件（子装配体不进表），机加件按与总装同级判定、外购件按子文件夹判定，数量含嵌套倍数且抑制实例不计，同名同目录 `.SLDDRW` 判为该零件的工程图；外购件只认边界那一层——外购装配体的内部件（含深层后代、含碰巧存在主目录里的）既不进外购件清单也不进机加件清单，`HasPurchasedAncestor` 按实例名整段前缀判祖先、自己不算自己的祖先且 `气缸-10` 不是 `气缸-1` 的孩子；外购件「规格 / 名称」按中文与非中文切分（`深沟球轴承 6205` → 规格 `6205`、名称 `深沟球轴承`），无中文时名称留空；只有机加件进 STEP 作业，一张工程图产生 DWG 与 PDF 两个作业，`Overwrite` 为 true；四个打包目录一次建齐（含本轮为空的那些）；两张 BOM 落在 `BOM/` 且文件名带去掉数字段的总装前缀，机加件写 A/B/C/D 四列、外购件写 A/D/E/F 四列，模板样式与合并格随数据行下移，合计公式重指本轮数据区，`xl/calcChain.xml` 已删除 |
| VERIFY-UI | `dotnet run --project .\b-Code-HistoryMinerva-Tests\tests\HistoryMinerva.UiSmoke\HistoryMinerva.UiSmoke.csproj -c Release -p:NuGetAudit=false`（**不带参数即跑完整矩阵**；加 `--capture <目录>` 顺手留 PNG） | 输出 `HistoryMinerva.UiSmoke: PASS（4 个布局用例）`。矩阵写在 `Program.Matrix` 里、由工具自己遍历，不靠人记得跑几遍：320-dark / 760-dark / 1200-light / 1200-busy，加载 HistoryAurora Release 运行时的真实浅深主题。每档核对：窗口确实完成排版（宽高非 0），顶栏等宽且高度不低于 32px，来源与内容的标签同选择控件同列且标签在左，五种转换内容齐备，零件表铺满正文宽度且上方无独立标题行，装配结构默认收起、装配转换下「打开」按钮在选项区左侧，转换选项三项、页面无状态栏，宽窗口操作栏单行、窄窗口无水平溢出，运行态进度条亮且取消入口可点、空闲态两者相反，整体打包下三个开关隐藏且「特征」「草图」两列收起、主按钮为「打包」，生产 XAML 无固定十六进制颜色。**工具不得有任何等人的路径**：无论断言成败窗口都在 `finally` 里关闭，窗口开在屏幕外不打扰桌面，另有 3 分钟看门狗兜底 |
| VERIFY-FORMAT | `dotnet format .\HistoryMinerva.sln --verify-no-changes --no-restore` | 四个生产工程、Smoke 与 UI Smoke 均无格式差异 |
| VERIFY-PACKAGE | `powershell -NoProfile -ExecutionPolicy Bypass -File .\b-Code-HistoryMinerva\eng\Build-HistoryMinervaPackage.ps1` | HistoryVulcan 宿主不低于 5.1.2，七个运行产物和 SHA256SUMS 一致，无宿主 DLL、无 HistoryMinerva.Api、无 Roslyn |
| VERIFY-DEPLOY | 宿主 `vulcan.dev.submit` / `finish` 后复核 | `z-Publish/HistoryMinerva-vX.Y.Z/` 哈希与正式包一致；包内无 HistoryMinerva.Api.dll 与 Roslyn |
| VERIFY-CAD | `AssemblyProductionGate` / `FeatureWorksBatchSmoke` / `SolidWorksSelfPipelineGate` | `tools/` 只保留三个可重复执行的端到端门禁；Solid Edge → SolidWorks 链路本次不重跑，沿用 Worker/COM 未改变的既有真机证据 |
| VERIFY-SW-SELF | `SolidWorksSelfPipelineGate --assembly <源.SLDASM> --worker <HistoryMinerva.Worker.exe> --out-dir <输出目录>` | 退出码 0；实例数与源一致且全部配对，最大平移偏差 < 1e-6 m、最大旋转偏差 < 1e-9；产物装配 open errors=0；每个零件都有非空 Parasolid 文本 `.x_t` 落在 `XT/`；Worker 事件不含「跳过整备」或「源零件已有特征树」；产物不得与源零件字节相同；识别失败或几何被改坏的零件为 Parasolid 导入体而非源特征树；源目录文件与源装配 SHA256 一字未改 |
| VERIFY-PACK | **手工真机**：在装有 SolidWorks 的机器上，页面选「整体打包」→ 选含工程图的样件总装 → 解析装配体 → 打包 | `STP/`、`DWG/`、`PDF/`、`BOM/` 四目录齐备（含本轮为空的那些）；每个机加件一个非空 `.STEP`；每张同名工程图各一个非空 `.DWG` 与 `.PDF`，多页工程图的 PDF 必须包含全部图纸页；两张 BOM 能被 Excel 直接打开且行数与页面表格一致；**外购件清单里一个外购装配体只占一行**（内部零件不得各自成行）；打包前后源零件、源工程图与源装配的 SHA256 一字未改。**本项尚无自动化门禁工具**——`tools/` 下只有三个既有 CAD 门禁，打包链路的真机证据目前靠这一条手工步骤，离线 Smoke 覆盖的是计划、BOM 与作业构建，覆盖不到 `SaveAs3` 本身 |
| VERIFY-CI | `.github/workflows/historyminerva-gate.yml` | push/PR 上锁定还原、质量门禁、双配置构建与 Smoke、窄/宽 UI Smoke、格式、候选包及无 tracked diff 全绿 |

Smoke 还检查后台注册五条 `minerva.worker.*` 只读命令、四条 `minerva.conversion.*`（禁止 MCP）、
`minerva.ui.describe/data/actions/picksource` 描述式协议（禁止 MCP）且没有
`HistoryMinerva.*`；转换指令在无 ShellUi 的 Attach 阶段也必须登记，否则热重载后页面报未知指令。
UI Smoke 检查 Worker 进度、失败和取消均进入命令日志，页面不再显示全局提示。

VERIFY-SW-SELF 需要真实 SolidWorks + FeatureWorks 会话。FeatureWorks 的自动识别依赖**进程内激活态**：
该 SolidWorks 进程里必须先人工做过一次特征识别，否则整批只会产出压平后的导入体（几何与装配位置不受影响）。
本版真机证据见 `b-Office/history/45-V4.3.0-SW自整备转换管线.md` §5。带真实配合的 SW 装配体样件尚未取得，
SW 源的配合重建只有离线契约测试覆盖。

云端 CI 明确不启动 SolidWorks、Solid Edge 或 FeatureWorks，也不运行 `VERIFY-SW-SELF`；这些检查只有在
具备 CAD 授权、桌面会话、样件和 FeatureWorks 进程内激活态时才有有效结论。

正式发布默认先部署后提交。只有要从干净 HEAD 复现时，才为本次管线追加 `-RequireCleanSource`。
