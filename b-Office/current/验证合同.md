# HistoryMinerva 4.2.3 验证合同

| 编号 | 命令或步骤 | 通过标准 |
| --- | --- | --- |
| VERIFY-CONTRACT | `powershell -NoProfile -ExecutionPolicy Bypass -File .\b-Code\Test-ProjectContract.ps1 -Instantiation` | manifest、路径、链接、目录和占位符检查通过 |
| VERIFY-VERSION | 读取 `HistoryMinerva.Version.props`、源码 `module.manifest.json` 与正式 z 清单 | 源码、源码清单与正式 z 均为 4.2.3；旧 4.2.2 只存在于发布历史，不再让不同行为共享版本号 |
| VERIFY-BUILD | `dotnet build .\b-Code-HistoryMinerva\src\HistoryMinerva\HistoryMinerva.csproj -c Release -p:NuGetAudit=false` | 0 警告、0 错误，引用 HistoryVulcan.Core 3.4.0.0 且 `Private=false` |
| VERIFY-SMOKE | `dotnet run --project .\b-Code-HistoryMinerva-Tests\tests\HistoryMinerva.Smoke\HistoryMinerva.Smoke.csproj -c Release -p:NuGetAudit=false` | 输出 `HistoryMinerva.Smoke: PASS`，身份、上下文、命令、生命周期与 SWuse 协议路由通过 |
| VERIFY-UI | `dotnet run --project .\b-Code-HistoryMinerva-Tests\tests\HistoryMinerva.UiSmoke\HistoryMinerva.UiSmoke.csproj ...` | 加载真实 HistoryVulcan 浅/深主题，覆盖 320/760/1200px；顶栏等宽、来源/内容标签与选择控件同列，零件列表无独立标题行，宽窗口操作栏单行且窄窗口无水平溢出 |
| VERIFY-FORMAT | `dotnet format .\b-Code-HistoryMinerva\src\HistoryMinerva\HistoryMinerva.csproj --verify-no-changes --no-restore` | 无格式差异 |
| VERIFY-PACKAGE | `powershell -NoProfile -ExecutionPolicy Bypass -File .\b-Code-HistoryMinerva\eng\Build-HistoryMinervaPackage.ps1` | HistoryVulcan 3.4.0 版本与两项 SHA 固定，十个运行产物和 SHA256SUMS 一致，无宿主 DLL |
| VERIFY-DEPLOY | Diana 集中发布命令 `-Module HistoryMinerva -Publish` 后复核 | Z 快照哈希与正式包一致，旧 Mapping、SWuse 与历史转换模块槽退役且备份不在 Modules |
| VERIFY-CAD | `b-Code-HistoryMinerva-Tests/tools` 专用样件 | 本次不重跑；沿用 Worker/COM 未改变的既有真机证据 |

UI Smoke 还检查 `HistoryMinerva.convert/cancel` 的只前端、非只读、禁止 MCP 元数据以及服务宿主无 ShellUi 时不重复注册。

工作树存在未提交改动时，只有在用户明确授权当前版本部署的情况下，才允许为本次管线追加 `-AllowDirtySource`；正式发布通常应使用干净工作树。
