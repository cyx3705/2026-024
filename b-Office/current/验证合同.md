# Mapping 验证合同

## 验证层级

| 编号 | 命令或步骤 | 通过标准 |
| --- | --- | --- |
| VERIFY-CONTRACT | `powershell -NoProfile -ExecutionPolicy Bypass -File .\b-Code\Test-ProjectContract.ps1 -Instantiation` | manifest、路径、链接、目录和占位符检查通过 |
| VERIFY-BUILD | `dotnet build .\b-Code-SE2SW\src\SE2SW\SE2SW.csproj -c Release -p:NuGetAudit=false` | 0 警告、0 错误 |
| VERIFY-SMOKE | `dotnet run --project .\b-Code-SE2SW-Tests\tests\SE2SW.Smoke\SE2SW.Smoke.csproj -c Release -p:NuGetAudit=false` | 输出 `SE2SW.Smoke: PASS`，断言 Mapping 身份与两项映射 |
| VERIFY-UI | `dotnet run --project .\b-Code-SE2SW-Tests\tests\SE2SW.UiSmoke\SE2SW.UiSmoke.csproj -c Release -p:NuGetAudit=false -- --compact --capture <png>` | 两个等宽顶栏、两项映射、无 XT/SW 目录控件，布局和运行条稳定 |
| VERIFY-FORMAT | `dotnet format .\b-Code-SE2SW\src\SE2SW\SE2SW.csproj --verify-no-changes --no-restore` | 无格式差异 |
| VERIFY-CAD | 使用 `b-Code-SE2SW-Tests/tools` 和专用样件 | 几何、装配、哈希与生命周期满足技术合同 |
| VERIFY-DEPLOY | OHS `tool.scan`、`tool.sync name=Mapping`、`tool.list`、`module.list`、`command.list domain=mapping mcp=all` | 唯一 Mapping 来源、4.0.0、7/7 哈希一致、命令/MCP 已装载 |

## 真实 CAD 前提

- Windows x64，已安装并注册 Solid Edge 2020 和 SolidWorks 2025 SP5。
- 使用复制到临时目录的专用样件；FeatureWorks 档要求已人工激活的 SolidWorks 会话。
- 运行前后记录 CAD 进程，只核对和回收本次明确创建的进程。

本次改名与 UI 收敛不改变 Worker JSON 协议或 CAD COM 实现，因此不重复运行真实 CAD 门禁；保留现有
真机证据，并完成合同、Release、Smoke、UI Smoke、格式和 OHS 装载核验。
