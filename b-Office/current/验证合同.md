# HistoryMinerva 4.3.1 验证合同

| 编号 | 命令或步骤 | 通过标准 |
| --- | --- | --- |
| VERIFY-CONTRACT | `powershell -NoProfile -ExecutionPolicy Bypass -File .\b-Code\Test-ProjectContract.ps1 -Instantiation` | manifest、路径、链接、目录和占位符检查通过 |
| VERIFY-VERSION | 读取 `HistoryMinerva.Version.props`、源码 `module.manifest.json` 与正式 z 清单 | 源码与源码清单均为 4.3.1；正式 z 为 4.3.0（含 SW 自整备但尚无新命令面），只有经批准的发布事务才能追上 |
| VERIFY-BUILD | `dotnet build .\b-Code-HistoryMinerva\src\HistoryMinerva\HistoryMinerva.csproj -c Release -p:NuGetAudit=false` | 0 警告、0 错误，引用 HistoryVulcan.Core 3.5.0.0 且 `Private=false` |
| VERIFY-SMOKE | `dotnet run --project .\b-Code-HistoryMinerva-Tests\tests\HistoryMinerva.Smoke\HistoryMinerva.Smoke.csproj -c Release -p:NuGetAudit=false` | 输出 `HistoryMinerva.Smoke: PASS`，身份、上下文、命令、生命周期、SWuse 协议路由与 SW 自整备契约通过 |
| VERIFY-UI | `dotnet run --project .\b-Code-HistoryMinerva-Tests\tests\HistoryMinerva.UiSmoke\HistoryMinerva.UiSmoke.csproj ...` | 加载真实 HistoryVulcan 浅/深主题，覆盖 320/760/1200px；顶栏等宽、来源/内容标签与选择控件同列，零件列表无独立标题行，宽窗口操作栏单行且窄窗口无水平溢出 |
| VERIFY-FORMAT | `dotnet format .\b-Code-HistoryMinerva\src\HistoryMinerva\HistoryMinerva.csproj --verify-no-changes --no-restore` | 无格式差异 |
| VERIFY-PACKAGE | `powershell -NoProfile -ExecutionPolicy Bypass -File .\b-Code-HistoryMinerva\eng\Build-HistoryMinervaPackage.ps1` | HistoryVulcan 3.5.0 版本与两项 SHA 固定，十个运行产物和 SHA256SUMS 一致，无宿主 DLL |
| VERIFY-DEPLOY | Diana 集中发布命令 `-Module HistoryMinerva -Publish` 后复核 | Z 快照哈希与正式包一致，旧 Mapping、SWuse 与历史转换模块槽退役且备份不在 Modules |
| VERIFY-CAD | `b-Code-HistoryMinerva-Tests/tools` 专用样件 | Solid Edge → SolidWorks 链路本次不重跑；沿用 Worker/COM 未改变的既有真机证据 |
| VERIFY-SW-SELF | `SolidWorksSelfPipelineGate --assembly <源.SLDASM> --worker <HistoryMinerva.Worker.exe> --out-dir <输出目录>` | 退出码 0；实例数与源一致且全部配对，最大平移偏差 < 1e-6 m、最大旋转偏差 < 1e-9；产物装配 open errors=0；退回源副本的零件与源逐字节相同；源目录文件与源装配 SHA256 一字未改 |

Smoke 还检查后台只注册五条 `minerva.worker.*` 只读命令且没有 `HistoryMinerva.*`；UI Smoke 检查
`minerva.conversion.probe/run/cancel` 的只前端、禁止 MCP 元数据、Worker 进度进入命令日志，以及服务宿主无 ShellUi 时不重复注册。

VERIFY-SW-SELF 需要真实 SolidWorks + FeatureWorks 会话。FeatureWorks 的自动识别依赖**进程内激活态**：
该 SolidWorks 进程里必须先人工做过一次特征识别，否则整批只会退回源零件副本（几何与装配位置不受影响）。
本版真机证据见 `b-Office/history/45-V4.3.0-SW自整备转换管线.md` §5。带真实配合的 SW 装配体样件尚未取得，
SW 源的配合重建只有离线契约测试覆盖。

工作树存在未提交改动时，只有在用户明确授权当前版本部署的情况下，才允许为本次管线追加 `-AllowDirtySource`；正式发布通常应使用干净工作树。
