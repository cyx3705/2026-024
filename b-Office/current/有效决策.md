# HistoryMinerva 有效决策

## DEC-001 CAD 自动化使用独立 x64 STA Worker

- 状态：已接受
- 日期：2026-08-05
- 决策：OHS UI 不持有 CAD COM 对象，Solid Edge、SolidWorks 和 FeatureWorks 调用全部在独立 Worker 中执行。
- 原因：隔离线程模型、崩溃、超时和 CAD 生命周期。

## DEC-002 运行时发现厂商组件

- 状态：已接受
- 日期：2026-08-05
- 决策：通过 COM 注册、安装目录和有效物理模板探测环境，不提交厂商 Interop DLL，不硬编码机器路径。
- 原因：兼容安装差异并保持发布包权责清晰。

## DEC-003 几何正确性优先

- 状态：已接受
- 日期：2026-08-05
- 决策：FeatureWorks 或配合重建不能通过守卫时，回退到几何完整、位置正确的结果并报告降级。
- 原因：错误特征树或漂移装配比可见降级更危险。

## DEC-004 保持隐藏 MCP 暴露

- 状态：已接受
- 日期：2026-08-05
- 决策：`mcpExposure=readonly`，真实 CAD 转换只从本机 UI 触发。
- 原因：转换依赖用户文件选择、CAD 会话和可见确认。

## DEC-005 版本只有一个人工真值

- 状态：已接受
- 日期：2026-08-05
- 决策：只编辑 `b-Code-HistoryMinerva/build/HistoryMinerva.Version.props`，程序集和模块清单版本从该文件派生。
  （4.2.1 对齐：版本文件随模块改名，决策本意不变。）
- 原因：避免 UI、Worker、Contracts 与 OHS 清单漂移。

## DEC-006 独立于 Studio工具箱维护

- 状态：已接受
- 日期：2026-08-05
- 决策：源码、项目合同和 OHS 清单唯一来源迁为 `2026-024-HistoryMinerva`（由原转换项目迁入），
  继承自 `0000-001-AIReady`。
- 原因：HistoryMinerva 已形成独立目标、发布线、验证体系和长期维护边界。
- 后果：`2026-019-Studio工具箱` 不再保留 HistoryMinerva 源码或清单，历史记录保留原来源事实。

## DEC-007 生产代码与验证代码分根维护

- 状态：已接受
- 日期：2026-08-05
- 决策：生产模块保留在 `b-Code-HistoryMinerva`；Smoke、UI Smoke、COM 探针和真实 CAD 门禁统一放在
  `b-Code-HistoryMinerva-Tests`。（4.2.1 对齐：两个根目录随模块改名，分根本意不变。）
- 原因：测试工程已形成独立依赖、运行环境和生命周期，不应继续混入发布模块目录。
- 后果：测试工程通过显式 ProjectReference 消费生产源码，HistoryVulcan 模块清单只引用生产构建输出。

## DEC-008 直接消费 HistoryVulcan 正式宿主快照

- 状态：已接受
- 日期：2026-08-08
- 决策：Mapping 生产工程直接引用 HistoryVulcan 3.3.0 正式快照中的 `HistoryVulcan.Core.dll`，设置
  `Private=false`；发布时固定宿主 manifest 与 Core SHA，并同时部署前端/服务端槽。
- 原因：模块公共接口、运行时上下文、命令总线和主题必须与实际宿主保持同一份精确合同。
- 后果：正式包不私带 HistoryVulcan DLL；快照版本或 SHA 变化时，发布脚本拒绝复用旧构建，必须重新验证。

## DEC-009 并入 SWuse 并改名 HistoryMinerva

- 状态：已接受
- 日期：2026-08-08
- 决策：SWuse 0.1.1（源码 `b-Code-SWuse`、清单 `z-SWuse`）自 `2026-019-Studio工具箱` 整体并入本项目，
  项目目录改名 `2026-024-HistoryMinerva`；两个模块保持各自对外名称（`Mapping`/`mapping`、
  `SWuse`/`swuse`）、版本源、程序集与发布链不变。SWuse 继续经 NuGet 引用 `OneHistory.HistoryVulcan.Core`
  3.0.3，`b-Code-SWuse/nuget.config` 显式声明 023 本地 feed；对齐 3.3.0 列为后续独立版本。
- 原因：两模块同构（Contracts + x64 STA Worker + HistoryVulcan 模块清单），同仓维护降低治理成本；
  保持既有身份与宿主合同避免功能回归。
- 后果：`2026-019-Studio工具箱` 不再保留 SWuse 源码或清单；合并细节见
  `b-Office/history/42-V4.1.0-HistoryMinerva模块合并计划.md`。
- 修订（同日）：本条中“两模块保持各自对外名称、版本源与发布链”由 DEC-010 废止——4.2.1 起
  合并为单一模块、单一版本源与单一发布目录；NuGet 3.0.3 对齐也在 DEC-010 一并完成。

## DEC-010 4.2.1 前后端分离：双模块合并为单模块 HistoryMinerva

- 状态：已接受
- 日期：2026-08-08
- 决策：Mapping 与 SWuse 合并为单一模块 `HistoryMinerva`（模块名与命令域统一为
  `HistoryMinerva`，全部取自 `HistoryMinervaIdentity` 唯一权威源；宿主命令表忽略大小写，
  小写输入仍可用，无兼容别名）。停靠页标题取短形 `Minerva`。前端只保留原 Mapping 中央
  停靠页；SWuse 独立窗口删除（界面打磨方向未定），其 C# 建模能力保留在协议层。
  所有操作代码并入单一 `HistoryMinerva.Worker.exe`，入口按参数形态路由 HistoryMinerva（4 参数）/
  SWuse（2 参数）两条既有协议。发布合并为 `z-HistoryMinerva` 单一目录（清单与运行产物同处，
  不再另设包目录）；测试全部并入 `b-Code-HistoryMinerva-Tests`（Smoke 合一、探针与 CadGate
  归拢 tools/）。
  4.2.2 修订：内部程序集与命名空间统一为 `HistoryMinerva.*`/`SWuse.*`，移除旧转换模块命名，
  不保留兼容别名。
  SWuse 一并完成 DEC-009 遗留的 HistoryVulcan 3.3.0 对齐（改用钉定快照，弃用 NuGet 3.0.3）。
- 原因：双模块同构且共享 Worker 骨架，单模块单发布单元降低部署与治理成本；
  SWuse 窗口价值未定，先退到协议层，避免为未打磨的 UI 维护宿主集成面。
- 后果：旧 `Mapping`/`SWuse` 与 OneHistoryStudio 历史转换模块槽在部署时一并退役；
  `mapping.*`、`swuse.*` 指令域作废；数据目录随模块改名迁移（`HistoryVulcan/HistoryMinerva`、
  `HistoryMinerva/HistoryMinerva`）；SWuse 0.1.x 版本线终止；`z-Package-HistoryMinerva`
  包目录取消（同日对齐修订）。细节见 `b-Office/history/44-V4.2.0-前后端分离重构计划.md`。

## DEC-011 4.2.3 隔离重构线与 4.2.2 正式快照

- 状态：已接受
- 日期：2026-08-10
- 决策：新增五命令与新 UI 的当前源码、源码清单和候选消费合同统一升为 `4.2.3`；
  已发布的 `z-HistoryMinerva` `4.2.2` 保持不可变，继续代表三命令旧形态，直到用户显式批准
  4.2.3 正式发布。Smoke 只比较版本权威源与源码清单，不再要求开发线和正式 z 快照同号。
- 原因：同一个 `4.2.2` 曾同时指代正式三命令包和工作树五命令重构，消费者无法仅凭版本判断合同。
- 后果：源码行为或消费面发生变化时必须先分配新版本；正式 z 只有通过发布事务才能追上源码版本，
  不得为让测试通过而直接改写正式快照。
- 执行：2026-08-10 已获用户显式批准并通过 Diana 集中发布事务将正式 z 提升为 `4.2.3`；
  旧 `4.2.2` 已转入发布历史，本决策的版本隔离规则继续有效。

## DEC-012 4.2.3 对齐 HistoryVulcan 3.4.0 正式宿主

- 状态：已接受
- 日期：2026-08-10
- 取代：DEC-008 中固定 HistoryVulcan 3.3.0 的版本字面量；直接消费正式快照、`Private=false`
  和发布前核验宿主身份的原则继续有效。
- 决策：`HistoryMinerva.Version.props`、项目 manifest、当前合同与发布验证统一钉定
  `2026-023-HistoryVulcan/z-HistoryVulcan` 3.4.0；manifest 和 Core SHA 由项目 manifest 记录。
- 原因：正式宿主已经提升到 3.4.0，继续要求 3.3.x 会让正常候选构建在引用解析前必然失败。
- 后果：4.2.3 候选和正式包只能由 3.4.0 快照构建；模块包仍不得私带 HistoryVulcan DLL。
- 修订：版本字面量已由 DEC-014 提升到 3.5.0；直接消费正式快照的原则继续有效。

## DEC-013 SolidWorks 源走"就地整备"，不复用 Solid Edge 的中转往返

- 状态：已接受
- 日期：2026-08-11
- 决策：源格式由 `ConversionSourceFormat` 承载（缺省 `SolidEdge`，作为可选字段追加在四个请求记录
  末尾），分叉只发生在读装配与取零件两步；SolidWorks 源不经 Parasolid 中转，零件在**副本**上
  就地识别，识别不可用时产物退回与源逐字节相同的零件副本。此后的拓扑排序、嵌套装配生成、
  变换校验、配合重建与复用判定，两种源共用同一份实现。
- 原因：源已经是 `.SLDPRT`，再绕一圈 XT 只会引入中转误差和一整套本可以不存在的失败模式；
  而"同形读数 + 共用下游"让新源格式不必复制任何既有逻辑，也就不会分叉出第二套行为。
- 后果：SW 源不产生 `.x_t`、不创建 `XT/` 目录、不校验 `XtPath`；降级产物字节等于源文件，
  比"重新导入哑实体"更干净。历史请求 JSON 没有该字段，必须仍按 Solid Edge 执行——
  这个缺省值是全部向后兼容性的唯一依据。产物绝不能是源文件本身，计划器与 Worker 校验器各拦一次。
- 细节与真机证据见 `b-Office/history/45-V4.3.0-SW自整备转换管线.md`。

## DEC-014 4.3.0 对齐 HistoryVulcan 3.5.0 正式宿主

- 状态：已接受
- 日期：2026-08-11
- 取代：DEC-012 中固定 HistoryVulcan 3.4.0 的版本字面量；直接消费正式快照、`Private=false`
  和发布前核验宿主身份的原则继续有效。
- 决策：`HistoryMinerva.Version.props`、项目 manifest、当前合同与发布验证统一钉定
  `2026-023-HistoryVulcan/z-HistoryVulcan` 3.5.0；manifest 和 Core SHA 以版本 props 为唯一真源，
  项目 manifest 保存机器可读投影。
- 原因：正式宿主已提升到 3.5.0，继续钉 3.4.0 会让候选构建在引用解析前必然失败。
- 后果：4.3.0 候选和正式包只能由 3.5.0 快照构建；模块包仍不得私带 HistoryVulcan DLL。
  本次只做了版本对齐与全套离线验收，未对宿主 3.5.0 的新增消费面做专门评审。

## DEC-015 4.3.1 命令域与执行链统一归 Vulcan 总线

- 状态：已接受
- 日期：2026-08-11
- 决策：模块身份继续为 `HistoryMinerva`，命令域改为短形 `minerva`。后台以
  `IModuleContext.RegisterCommands` 显式注册 `minerva.worker.*`，不再让 `MainClassType` 反射生成
  `HistoryMinerva.*`；页面探查、转换和取消统一注册为 `minerva.conversion.*`，禁止页面在总线失败后
  直接执行 ViewModel。
- 原因：旧实现把 `ModuleCommand.CommandClass=worker` 当作命令名的一部分，但宿主实际只用它作元数据，
  导致命令列表显示像三段式，真实调用仍是 `HistoryMinerva.Status`；页面回退又绕开了总线日志和控制台。
- 后果：Worker 状态、实际路径与协议能力可从后台命令表和只读 MCP 查询；CAD 操作仍只允许前端执行，
  中间事件通过 `CommandContext.Progress` 进入 Vulcan 控制台。版本升为 4.3.1；正式 z 现已完成提升。

## DEC-016 以锁定构建图和双层门禁替代单一合同检查

- 状态：已接受
- 日期：2026-08-11
- 决策：参考 HistoryJanus，把 Minerva 验证拆为仓内 `Test-QualityGate.ps1` 与 GitHub Actions 推送/PR
  门禁。仓内门禁负责静态漂移、正式快照和宿主钉定；CI 负责锁定还原、双配置构建与 Smoke、UI、格式和候选包。
- 原因：旧 `verify` 只检查目录与文档结构，无法发现本轮已经出现的正式版本描述漂移和宿主 SHA 漂移；
  单跑 Release Smoke 也不能证明 Debug、格式、包边界或依赖可复现。
- 后果：云端 CI 只承担无 CAD 环境也能给出确定结论的检查；SolidWorks/FeatureWorks 与 Solid Edge
  真机验证继续使用专用样件和受控会话，不降低门槛，也不伪装成托管 CI 能力。

## DEC-017 4.3.2 用户提示只进入 Vulcan 命令控制台

- 状态：已接受
- 日期：2026-08-11
- 决策：Minerva 页面不再使用 `MessageBox` 或全局状态文本显示探查、转换、取消结果；这些结果、错误和
  Worker 中间进度统一由 `minerva.conversion.*` 通过 Vulcan `CommandBus` 写入控制台。零件行状态保留。
- 原因：页面消息与总线结果并存会造成重复提示，并且旧 ViewModel 会吞掉 Worker 异常，导致失败操作被总线
  记录为成功。
- 后果：ViewModel 的异常和取消必须传播至命令适配层；命令处理器显式返回 `CommandResult.Fail`，UI 模块不再
  直接写最终结果日志。源码版本升为 4.3.2，正式 `z-HistoryMinerva` 同步提升并纳入本轮门禁。

## DEC-018 4.3.2 对齐 HistoryVulcan 3.9.0 最低宿主

- 状态：已接受
- 日期：2026-08-11
- 取代：DEC-014 的当前宿主版本钉定；正式快照不可变和生产引用 `Private=false` 的原则继续有效。
- 决策：源码候选消费 `2026-023-HistoryVulcan/z-HistoryVulcan`，最低兼容版本为 3.9.0；宿主版本及程序集
  身份必须满足下限，宿主重建产生的 SHA 变化不再要求模块跟随改版。
- 原因：正式宿主已提升到 3.9.0，继续钉定 3.5.0 会让默认构建在引用解析前失败，也无法验证当前命令总线。
- 后果：源码和候选包只从不低于 3.9.0 的正式快照构建；正式 Minerva 4.3.2 快照不再绑定某一次宿主构建的 SHA，
  质量门禁验证当前宿主下限和正式快照自身一致性。

## DEC-019 真机工具只保留端到端门禁

- 状态：已接受
- 日期：2026-08-12
- 决策：`b-Code-HistoryMinerva-Tests/tools` 只保留 `AssemblyProductionGate`、`FeatureWorksBatchSmoke`、
  `SolidWorksSelfPipelineGate` 与 `SWuse.CadGate`。一次性探索探针在结论进入生产、Smoke 或历史证据后删除。
- 原因：矩阵、配合、FeatureWorks 参数和 COM 会话探索工具已形成约一万行第二套实现，重复生产 Gate 的能力，
  增加依赖、编译和维护成本，却不进入发布门禁。
- 后果：真机入口从 16 个收敛到 4 个；需要新增诊断时优先扩展现有 Gate 的报告能力，临时探针不得默认长期保留。

## DEC-020 旧平铺产物只属于 Solid Edge 源；"已存在"必须跟随识别开关

- 状态：已接受
- 日期：2026-08-12
- 决策：V3.0 时代的旧平铺产物（源目录里同基名的 `XT`/`SLDPRT`）只对 Solid Edge 源成立，由
  `ConversionPathLayout.HasLegacyFlatLayout` 唯一判定；SolidWorks 源不得回退到该路径。装配模式下
  开启特征识别时，已有产物一律重做，零件行状态显示"待整备"而非"已存在"。
  `PreflightValidator` 与 `WorkerRequestValidator` 保持同一口径：扩展名按源格式取，无 Parasolid
  中转时不校验 XT，只在源格式为 Solid Edge 时要求 Solid Edge COM。
- 原因：SolidWorks 源的"旧平铺产物路径"按构造就是源零件本身。回退分支把每个零件判成"已存在"，
  并把产物路径改写回源文件，计划阶段随即以"产物会覆盖源零件本身"整轮阻断——SW 自整备在默认输出
  目录下完全不可用。上一版用例传了自定义输出目录，正好关掉回退分支，因此没有暴露；而界面两个
  自定义目录传的都是 `null`。同时 `PreflightValidator` 未随 V4.3 更新，仍写死 `.par`/`.asm`。
- 后果：SW 自整备在默认 `SW/` 输出目录下可用；Solid Edge 的旧平铺复用行为不变。涉及产物存在性
  或源格式的用例必须覆盖 `customSolidWorksDirectory` 为 `null` 的参数组合，即界面真正产生的那一组。
  源码版本升为 4.3.3，正式 `z-HistoryMinerva` 同步提升；本轮只跑过离线门禁，真机 CAD 门禁未执行。

## DEC-021 特征识别的三条真机口径

- 状态：已接受
- 日期：2026-08-12
- 决策：
  1. **源零件没有导入体 = 无需整备**，在识别之前直接原样交付，且**不计入**"连续未识别"计数。
  2. **残留导入体属于部分识别**，只要建成了造型特征就保留整棵特征树，如实报"建成 N 个、另有 M 处未识别"；
     只有"报成功却零造型特征""几何被改坏""钣金误识别"才丢弃。
  3. **专属会话产不出特征**。会话故障后重试会起新 SolidWorks 进程，而 FeatureWorks 的自动识别要求该
     进程内先由人工识别过一次，新进程按定义未激活。该路径只保证几何落盘，重试时主动关闭识别并如实说明。
- 原因（2026-08-12 真机实测，12位消解器样件，SolidWorks 2025 SP5）：
  - `SetAdvancedOptions` 返回 false 有**两种**成因——会话未激活、活动文档里没有可识别的导入体。
    旧代码把两者都当成"未激活"。实测批次头两个零件恰好已整备好，连续两次"未识别"即触发整批降级，
    真正待整备的 BJ10B-04/05 一次都没被识别，12 个零件全部交付哑实体。
  - BJ10B-04 在已激活会话里识别 30 项并全部建成（16 旋转、6 孔向导、5 圆角），仅因残留一个
    `Imported1` 就被旧语义守卫整棵丢弃。守卫在扔掉好结果。
  - 同一零件同一参数：未激活会话识别 0 个，人工点过一次之后识别 30 个。激活状态是进程内状态，
    不跨进程、退出不保留。
- 后果：特征整备无法真正无人值守——每次 SolidWorks 重启后需人工先做一次识别。此约束应在界面和
  文档中明示，不能让用户跑完整批才发现全是哑实体。批次收尾必须重述该结论。
- 验收口径：用户依据早期实测，约 30% 的零件本就无法识别出特征；当前质量已达预期，本轮不再追加修复。
- 保留事项（非缺陷，供后续决策）：几何守卫阈值 `MaximumFeatureWorksVolumeRelativeDeviation = 2e-5`
  标定自 Solid Edge 的 XT 导入件，从未针对 SW 自整备验证。实测 BJ10B-04 偏差 4.77e-4、
  BJ10B-05 偏差 2.28e-4，面数 180→106、110→70，双双被拦回哑实体。

  这一类要与"无法识别"区分开：这两件 FeatureWorks **识别成功且特征全部建成**
  （BJ10B-04 共 50 项，含 16 旋转、6 孔向导、5 圆角；BJ10B-05 共 24 项），
  丢弃它们的是本项目的几何守卫，不是 FeatureWorks 的能力上限。
  若后续判定该体积差属拓扑清理而非几何丢失，按 SW 自整备实测值重新标定阈值即可收回这部分特征树；
  判定之前不得放宽——放宽几何守卫的后果会传导到制造端。

## DEC-022 识别结果的取舍按"人工还能不能救"定

- 状态：已接受
- 日期：2026-08-12
- 取代：DEC-021 中关于"残留导入体属部分识别、予以保留"的判据，以及几何守卫的存在本身。
- 决策：
  1. **残留导入体一律丢弃**。带导入体的零件不合规，而且没有特征可编辑、**人工修不回来**，
     只能整件重做。交出去只会浪费下游时间，不如退回哑实体并提示单独处理。
  2. **识别得不完美但特征完备的一律放行**。特征类型认错、体积有偏差、尺寸标注不理想
     这类问题**人工可以改**，拦下来反而剥夺了用户修正的机会。
  3. **几何守卫（识别前后比对体积与面数，阈值 2e-5）整条移除**，包括
     `DescribeGeometryMismatch` 与其阈值常量。体积偏差属于第 2 类，不属于第 1 类。
- 原因：判据此前按"识别得像不像手工结果"定，与产品的实际验收标准不是一回事。
  实测 BJ10B-05消解盖识别出 14 个特征、草图 10/10 完全定义、无残留导入体——完全可交付的形态——
  却因体积偏差 2.28e-4 超过 2e-5 被拦回哑实体。同一版里 BJ10B-04消解底壳残留一个导入体
  却会被放行，方向正好反了。
- 后果（2026-08-12 真机验收，12位消解器样件）：
  - BJ10B-05：识别 14 个特征、草图 10/10，产物与源不同，**整备成功**；
  - BJ10B-04：识别 30 个特征但残留 1 处导入体，**按口径拦截**，诊断说明"不合规且无法人工修复"；
  - 其余 10 个已有特征树的零件跳过整备，与源逐字节相同；
  - 装配 29/29 实例，位置偏差 1.7e-18。
- 保留：`FeatureOutcome.GeometryChanged` 字段保留但不再有任何代码置位，作为将来若需恢复
  几何判据的挂点；不得据此推断当前存在几何校验。

## DEC-023 SW 自整备的识别输入改走 Parasolid 往返

- 状态：已接受
- 日期：2026-08-12
- 取代：DEC-013 中"SolidWorks 源不经 Parasolid 中转、在副本上就地识别"的部分；
  该决策的其余内容（源格式由 `ConversionSourceFormat` 承载、下游共用同一份实现、
  产物绝不能是源文件本身）继续有效。
- 决策：待整备零件在识别前先由已打开的副本导出 `.x_t`，再 `LoadFile4` 导回，
  在导回的文档上执行识别；识别成功后另存为 `.SLDPRT`。导出实现为
  `SolidWorksXtExporter.SaveAsParasolid`，与批量导出共用同一套失败判据。
- 原因：用户依据早期实测坚持这一步——直接对 `.SLDPRT` 识别的成品率不足。
- 边界（刻意保留，勿在后续改动中丢失）：
  1. **只有纯导入体才走 XT**。已有特征树的零件在更早一步就跳过整备，因此往返
     压平配置、自定义属性、Toolbox 配置的风险对本分支为零——那些零件本就没有可丢的东西。
  2. **失败回退仍是源文件逐字节副本**。XT 只作识别的输入通道，不参与失败路径。
  3. 导出后删除临时 `.SLDPRT`，避免识别产物另存时撞上同名文件。
- 证据现状（必须如实记载）：2026-08-12 的 12位消解器样件**无法判定本步的收益**。
  该模型 12 个零件里 10 个已带特征树、根本不进 XT 分支，真正走到的只有
  BJ10B-04 与 BJ10B-05 两件，而这两件走 XT 与直连的结果三项指标完全持平
  （识别 14 / 草图 10/10；识别 30 / 残留导入体）。XT 步骤确实执行——产物字节由
  205717 变为 202779、残留体命名由 `Imported1` 变为 `Imported2` 可证。
  本步按用户实测经验采纳，尚缺一批**未整备哑实体**上的对照数据；补齐前不得据本条
  宣称 XT 往返提升了识别质量，也不得据本批持平结果将其移除。
