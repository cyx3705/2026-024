# SE2SW 历史资料说明

本目录直接保存从 `2026-019-Studio工具箱` 迁入的版本设计、COM 研究、探针结论、真机验收和发布收口记录，
不再增加 `SE2SW` 子目录。这些文件保留当时的路径、版本和来源描述，不能覆盖 `../current` 中的现行合同。

历史发布收口仍是版本证据；当前可执行的 OHS 与 NuGet 发布合同位于 `../package`。常规任务不读取历史目录。
确需追溯时，只读取与指定版本或问题直接相关的最小文件集合；历史中的相对链接、
绝对机器路径和旧发布来源只作为证据，不表示当前项目布局。
